import { StdFieldDirective } from './std-field.directive';

describe('StdFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new StdFieldDirective();
    expect(directive).toBeTruthy();
  });
});
