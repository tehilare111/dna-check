import { MakatCopyDirective } from './makat-copy.directive';

describe('MakatCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new MakatCopyDirective();
    expect(directive).toBeTruthy();
  });
});
